```tsx
async function onUserLoginWithEmail() {
  const { user } = await mirrorworld.loginWithEmail({
    email: "email@example.com",
    password: "super-secret-password",
  })
  console.log(user)
}
```
