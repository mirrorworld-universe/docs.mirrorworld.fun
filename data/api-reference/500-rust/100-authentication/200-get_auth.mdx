---
title: get_auth
---

# `get_auth`
Retreives the user's authentication token from the SDK instance.

```rust
use mirrorworld_sdk_rust::{ get_auth };

fn main() {
    // get auth token
    let token = get_auth();
}
```
