---
title: get_network
---

# `get_network`
Retreives the current network from the SDK instance. This will either be
set to `mainnet` or `devnet`.

```rust
use mirrorworld_sdk_rust::{ get_network };

fn main() {
    // get auth token
    let token: &str = get_network();
}
```
