---
title: MirrorWorld.EVM.TransferToken
---

# `TransferToken`

Transfer token to another wallet.

```csharp
string tokenMint = "token mint";
int decimals = 9;
int amount = 1;
string toPublicKey = "to publick key";

MirrorWorld.EVM.TransferToken(tokenMint, decimals, amount, toPublicKey, null, (res) => {
    //todo
});
```