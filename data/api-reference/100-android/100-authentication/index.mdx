---
title: Authentication
---

# Authentication Methods | Android API Reference

- [`MWSolana.initSDK`](/api-reference/android/authentication/MWSolana.initSDK)
- [`MWSolana.startLogin`](/api-reference/android/authentication/MWSolana.startLogin)
- [`MWSolana.guestLogin`](/api-reference/android/authentication/MWSolana.guestLogin)
- [`MWSolana.isLoggedIn`](/api-reference/android/authentication/MWSolana.isLoggedIn)
- [`MWSolana.loginWithEmail`](/api-reference/android/authentication/MWSolana.loginWithEmail)
- [`MWSolana.fetchUser`](/api-reference/android/authentication/MWSolana.fetchUser)
- [`MWSolana.queryUser`](/api-reference/android/authentication/MWSolana.queryUser)
- [`MWSolana.logout`](/api-reference/android/authentication/MWSolana.logout)
- [`MWEVM.initSDK`](/api-reference/android/authentication/MWEVM.initSDK)
- [`MWEVM.startLogin`](/api-reference/android/authentication/MWEVM.startLogin)
- [`MWEVM.guestLogin`](/api-reference/android/authentication/MWEVM.guestLogin)
- [`MWEVM.isLoggedIn`](/api-reference/android/authentication/MWEVM.isLoggedIn)
- [`MWEVM.loginWithEmail`](/api-reference/android/authentication/MWEVM.loginWithEmail)
- [`MWEVM.fetchUser`](/api-reference/android/authentication/MWEVM.fetchUser)
- [`MWEVM.queryUser`](/api-reference/android/authentication/MWEVM.queryUser)
- [`MWEVM.logout`](/api-reference/android/authentication/MWEVM.logout)


## Authentication Tips

### Developer must choose which chain he wants to use
Developers must use specified classes to call APIs on specified chains.  
*For example, if they want to call init API on the BNB chain, they must call MWEVM.initSDK().*

>And this is their correspondence:
>- Solana:MWSolana
>- Ethereum:MWEVM
>- Polygon:MWEVM
>- BNB Chain:MWEVM

### User login with different social accounts.
Mirror World uniquely identifies a user by their email address. Whereas you may login with different accounts,
if the email address associated with that social account is different,
then we shall create a new account for the users. Likewise, the wallets associated with those accounts are not the same.

### Login invalidation

Authentication tokens saved locally by SDK would be out of date. So when a user opens the app, the developer should check his authentication every time. If the token is invalidated, we should guide users to login again.
For example, you can follow the logic like this:

```java
MirrorSDK.getInstance().CheckAuthenticated(new BoolListener() {
    @Override
    public void onBool(boolean boolValue) {
        boolean isLoggedIn = boolValue;
        if(isLoggedIn){
            //todo: enter content directly
        }else {
            //todo: show a button leading the user to login.
        }
    }
});
```
