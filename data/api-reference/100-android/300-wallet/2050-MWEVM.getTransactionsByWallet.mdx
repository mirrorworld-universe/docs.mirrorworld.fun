---
title: MWEVM.getTransactionsByWallet
chain: EVM
---

# `getTransactionsByWallet`
Get the transactions of user's wallet.

```java
String walletAddress = "wallet address";
int limit = 1;

MWEVM.getTransactionsByWallet(walletAddress, limit, new MirrorCallback() {
    @Override
    public void callback(String result) {
        //todo
    }
});
```
