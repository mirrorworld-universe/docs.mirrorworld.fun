---
title: MWEVM.transferETH
chain: EVM
---

# `transferETH`
Transfer ETH to wallet address.

```java
Activity activity = this;
String nonce = "nonce";
String gasPrice = 1;
String gasLimit = 1;
String to = "to address";
int decimals = 2;

MWEVM.transferETH(activity, nonce, gasPrice, gasLimit, to, decimals, new MirrorCallback() {
    @Override
    public void callback(String result) {
        //todo
    }
});
```
