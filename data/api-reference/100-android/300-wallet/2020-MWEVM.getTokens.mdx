---
title: MWEVM.getTokens
chain: EVM
---

# `getTokens`
Get the current user's tokens.

```java
MWEVM.getTokens(new MirrorCallback() {
    @Override
    public void callback(String result) {
        //todo
    }
});
```
