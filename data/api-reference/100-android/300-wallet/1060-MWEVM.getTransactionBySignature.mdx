---
title: MWEVM.getTransactionBySignature
chain: EVM
---

# `getTransactionBySignature`
Get the transactions by signature.


```java
String signature = "signature";

MWEVM.getTransactionBySignature(signature, new MirrorCallback() {
    @Override
    public void callback(String result) {
        //todo
    }
});
```
