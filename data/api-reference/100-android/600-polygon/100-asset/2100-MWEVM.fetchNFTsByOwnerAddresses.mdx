---
title: fetchNFTsByOwnerAddresses

---

### `fetchNFTsByOwnerAddresses`

Fetch NFTs By Owner Addresses. Returns a detailed payload of all NFTs whose `owners`
are provided.

```java
String owner = "owner address";
int limit = 99;

MirrorWorld.Polygon.Asset.fetchNFTsByOwnerAddresses(owner, limit, new MirrorCallback() {
    @Override
    public void callback(String result) {
        //todo
    }
});
```
