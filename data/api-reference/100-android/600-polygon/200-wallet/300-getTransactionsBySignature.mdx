---
title: getTransactionsBySignature

---

# `getTransactionsBySignature`
Get the transactions by signature.


```java
String signature = "signature";

MirrorWorld.Polygon.Wallet.getTransactionsBySignature(signature, new MirrorCallback() {
    @Override
    public void callback(String result) {
        //todo
    }
});
```
