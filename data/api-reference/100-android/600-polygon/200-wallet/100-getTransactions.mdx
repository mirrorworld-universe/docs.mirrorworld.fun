---
title: getTransactions

---

# `getTransactions`
Get the transactions of user's wallet.


```java
int limit = 1;

MirrorWorld.Polygon.Wallet.getTransactions(limit, new MirrorCallback() {
    @Override
    public void callback(String result) {
       //todo
    }
});
```
