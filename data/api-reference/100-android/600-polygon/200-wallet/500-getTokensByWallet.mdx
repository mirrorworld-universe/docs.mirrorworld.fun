---
title: getTokensByWallet

---

# `getTokensByWallet`
Get single transaction with its signature.

```java
String walletAddress = "wallet address";

MirrorWorld.Polygon.Wallet.getTokensByWallet(walletAddress, new MirrorCallback() {
    @Override
    public void callback(String result) {
        //todo
    }
});
```
