---
title: getTransactionBySignature

---

# `getTransactionBySignature`
Get the transactions by signature.


```java
String signature = "signature";

MirrorWorld.Solana.Wallet.getTransactionBySignature(signature, new MirrorCallback() {
    @Override
    public void callback(String result) {
        //todo
    }
});
```
