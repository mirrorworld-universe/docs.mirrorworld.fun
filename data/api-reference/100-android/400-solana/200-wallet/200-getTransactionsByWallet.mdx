---
title: getTransactionsByWallet
chain: Solana
---

# `getTransactionsByWallet`
Get the transactions of user's wallet.

```java
String walletAddress = "wallet address";
int limit = 1;
String nextBefore = "nextBefore";

MirrorWorld.getTransactionsByWallet(walletAddress, limit, nextBeforeStr, new MirrorCallback() {
    @Override
    public void callback(String r) {
        //todo
    }
});
```
