---
title: getTransactionByDigest
---

# `getTransactionByDigest`
Get transaction by a digest.

```swift
MWSDK.SUI.Wallet.getTransactionByDigest(digest:digest,onSuccess: { data in
    print(response)
},onFailed: { code, message in
    print("failed~")
})
```