---
title: getMintedNFTOnCollection
description: This endpoint is used to fetch NFTs on collection that user already minted
---

# `getMintedNFTOnCollection`
This endpoint is used to fetch NFTs on collection that user already minted

```js
let collectionAddress;

await mirrorworld.SUI.Asset.getMintedNFTOnCollection(collectionAddress)
```
